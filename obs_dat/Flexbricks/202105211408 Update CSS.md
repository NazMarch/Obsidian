#flexbricks 

1. First download Ruby `without devkit` [ruby](https://rubyinstaller.org/downloads/)
2. Then run installer and use standard instructions 
3. In the end of installing `don't use` additional installs
4. Open ruby prompt and run command

```ad-success
title: commands for ruby prompt
collapse: open
```bash
> gem install sass #to install sass

> sass -v #to check if sass was installed

> cd C:\YuniquePLM_Development\Software\padawan\Application\plmOn2\System

# if you need to compile css once - run command below
> sass -w sass:CSS/mdltheme
# where -w means "watching changes"
# CSS/mdltheme - the path to css files
```
