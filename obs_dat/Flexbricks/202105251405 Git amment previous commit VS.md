#flexbricks 

1. Stay in local branch
2. Make changes
3. Stage changes
4. Select `actions -> amment previous commit`
 If use git bash: `git commit --ammend`
6. Select push or `git push --force`
1. remove remote branch
2. select push


